# Strapi API (baked into the client at Vite build time — VITE_* prefix)
VITE_API_URL="http://localhost:1337/api"

# Formspree endpoint for the contact form (server-only — do not use VITE_ prefix)
FORMSPREE_URL="https://formspree.io/f/mojvppgn"

# Canonical site origin for robots.txt / sitemap.xml (server-only)
SITE_URL="https://www.book-store.com.pl"
